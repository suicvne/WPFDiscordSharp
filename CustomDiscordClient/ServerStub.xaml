<UserControl x:Class="CustomDiscordClient.ServerStub"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomDiscordClient"
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="32">
    <UserControl.Resources>
        <Style x:Key="ImageStyle1" TargetType="{x:Type Image}">
            <Setter Property="FlowDirection" Value="LeftToRight"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Canvas HorizontalAlignment="Left" Height="32" VerticalAlignment="Top" Width="32" Background="#FF937979"/>
        <Image x:Name="serverIcon" HorizontalAlignment="Left" Height="32" VerticalAlignment="Top" Width="32" Style="{DynamicResource ImageStyle1}"/>
    </Grid>
</UserControl>
